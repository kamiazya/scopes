<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <!-- Suppress status messages -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />
    <!-- Console appender with colored output for better readability -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender with daily rotation -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR:-${user.home}/.local/share/scopes/logs}/scopes.log</file>
        <append>true</append>
        <immediateFlush>true</immediateFlush>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover with max 30 days history -->
            <fileNamePattern>${LOG_DIR:-${user.home}/.local/share/scopes/logs}/scopes.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Async appender for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- Logger configuration for Scopes application -->
    <logger name="io.github.kamiazya.scopes" level="DEBUG"/>
    
    <!-- SQLDelight query logging -->
    <logger name="app.cash.sqldelight" level="INFO"/>
    
    <!-- Koin DI framework -->
    <logger name="org.koin" level="INFO"/>
    
    <!-- SQLite JDBC driver -->
    <logger name="org.sqlite" level="WARN"/>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="ASYNC_FILE"/>
    </root>
</configuration>